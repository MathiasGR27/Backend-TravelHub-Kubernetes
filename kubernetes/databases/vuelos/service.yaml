apiVersion: v1
kind: Service
metadata:
  name: postgres-vuelos
  namespace: travelhub

spec:
  selector:
    app: postgres-vuelos

  ports:
    - name: postgres
      port: 5432
      targetPort: 5432

  type: ClusterIP