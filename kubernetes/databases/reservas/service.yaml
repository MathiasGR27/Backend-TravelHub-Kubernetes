apiVersion: v1
kind: Service
metadata:
  name: postgres-reservas
  namespace: travelhub

spec:
  selector:
    app: postgres-reservas

  ports:
    - name: postgres
      port: 5432
      targetPort: 5432

  type: ClusterIP