apiVersion: v1
kind: Service
metadata:
  name: reservas-service
  namespace: travelhub

spec:
  selector:
    app: reservas-service

  ports:
    - name: http
      port: 4003
      targetPort: 4003

  type: ClusterIP