apiVersion: v1
kind: Service
metadata:
  name: vuelos-service
  namespace: travelhub

spec:
  selector:
    app: vuelos-service

  ports:
    - name: http
      port: 4002
      targetPort: 4002

  type: ClusterIP