apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-pagos
  namespace: travelhub

spec:
  replicas: 1

  selector:
    matchLabels:
      app: postgres-pagos

  template:
    metadata:
      labels:
        app: postgres-pagos

    spec:
      containers:
        - name: postgres-pagos

          image: postgres:17-alpine

          ports:
            - containerPort: 5432

          envFrom:
            - secretRef:
                name: postgres-pagos-secret

          volumeMounts:
            - name: postgres-pagos-data
              mountPath: /var/lib/postgresql/data

          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - pg_isready -U "$POSTGRES_USER" -d "$POSTGRES_DB"
            initialDelaySeconds: 5
            periodSeconds: 5

          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - pg_isready -U "$POSTGRES_USER" -d "$POSTGRES_DB"
            initialDelaySeconds: 20
            periodSeconds: 10

      volumes:
        - name: postgres-pagos-data
          persistentVolumeClaim:
            claimName: postgres-pagos-pvc