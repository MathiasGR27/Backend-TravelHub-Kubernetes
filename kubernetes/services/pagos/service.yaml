apiVersion: v1
kind: Service
metadata:
  name: pagos-service
  namespace: travelhub

spec:
  selector:
    app: pagos-service

  ports:
    - name: http
      port: 4004
      targetPort: 4004

  type: ClusterIP