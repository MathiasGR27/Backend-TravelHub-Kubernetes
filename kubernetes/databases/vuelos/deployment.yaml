apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-vuelos
  namespace: travelhub

spec:
  replicas: 1

  selector:
    matchLabels:
      app: postgres-vuelos

  template:
    metadata:
      labels:
        app: postgres-vuelos

    spec:
      containers:
        - name: postgres-vuelos

          image: postgres:17-alpine

          ports:
            - containerPort: 5432

          envFrom:
            - secretRef:
                name: postgres-vuelos-secret

          volumeMounts:
            - name: postgres-vuelos-data
              mountPath: /var/lib/postgresql/data

          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - pg_isready -U "$POSTGRES_USER" -d "$POSTGRES_DB"
            initialDelaySeconds: 5
            periodSeconds: 5

          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - pg_isready -U "$POSTGRES_USER" -d "$POSTGRES_DB"
            initialDelaySeconds: 20
            periodSeconds: 10

      volumes:
        - name: postgres-vuelos-data
          persistentVolumeClaim:
            claimName: postgres-vuelos-pvc